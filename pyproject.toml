[project]
name = "latynkatar_site"
version = "0.1.0"
description = "Узор ужывання бібліятэкі latynkatar, калекцыя карысных беларускіх спасылак."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.0",
    "gunicorn>=23.0.0",
    "latynkatar>=2.0.0",
]
